class ActivityModel {
  constructor(id, name, icon, unity, description, category, userid) {
    this.id = id;
    this.name = name;
    this.icon = icon;
    this.unity = unity;
    this.description = description;
    this.category = category;
    this.userid = userid;
  }
}

export default ActivityModel;
